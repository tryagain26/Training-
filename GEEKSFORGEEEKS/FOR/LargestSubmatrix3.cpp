#include <bits/stdc++.h>
using namespace std;
#define rep(i, a, b) for(int i = a; i <= b; i++)
int matrix[101][101];
int main(){
	int n, m;
	cin >> n >> m;
	rep(i, 1, n){
		rep(j, 1, m){
			cin >> matrix[i][j];
		}
	}
	

	rep(i, 1, n){
		rep(j, i, m){
			
		}
	}
}