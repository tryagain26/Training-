#include <bits/stdc++.h>
using namespace std;

int main(){
	int q, k;
	cin >> q >> k;
	vector<pair<int, int>> vp;
	while(q--){
		string s, x;
		cin >> s >> x;
		vp.push_back({s, x});
	}
	sort(vp.begin(), vp.end());
	int max = INT_MIN;
	// aqqqkk

	

}